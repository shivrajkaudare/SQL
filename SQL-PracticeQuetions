/* 
 Q.1 Cretae Database College, Create table name Teacher(id,name,subject,salary) and inser 4 records in it.
      1) Select Teachers whose salary is more than 55k.
      2) Rename the salary column to ctc.
      3) Update salary of all teachers by giving them an increment of 25%.
      4) Delete the salary column for teacher table.
*/

CREATE DATABASE IF NOT EXISTS College;
USE College;

CREATE TABLE Teacher(
 id INT PRIMARY KEY NOT NULL,
 name VARCHAR(20),
 subject VARCHAR(10),
 salary INT
);

INSERT INTO Teacher 
(id, name, subject,salary)
VALUES 
(23, "ajay", "math", 50000),
(47, "bharat", "english", 60000),
(18, "chetan", "chemistry", 45000),
(9, "divya", "physics", 75000);

/* 1) Select Teachers whose salary is more than 55k*/

  SELECT salary FROM Teacher
  WHERE salary > 55000;

/* 2) Rename the salary column to ctc*/
  ALTER TABLE Teacher
  CHANGE COLuMN salary ctc INT;

  /* 3) Update salary of all teachers by giving them an increment of 25% */
  UPDATE Teacher
  SET ctc = ctc + (0.25) * ctc;

  /* 3) Add a new column for teachers called city. the default city should be "Gurgaon"*/
  ALTER TABLE teacher
  ADD COLUMN City VARCHAR(20) DEFAULT "Gurgaon";

  /* 4) Delete the salary column for teacher table.*/
   ALTER TABLE Teacher
   DROP COLUMN Techsalary;



/*
  Q.2 Create table to store student info (roll_no, name, city, marks).
      insert following data in the table : 
              110,"adam","Delhi",76
              108,"bob","Mumbai",65
              124,"casey","pune",94
              112,"duke","pune",80

*/

   CREATE TABLE student(
   id INT PRIMARY KEY NOT NULL,
   name VARCHAR(30),
   city VARCHAR(30),
   marks INT
   );

  INSERT INTO student
  (id, name, city, marks)
  VALUES 
  (110,"adam","Delhi",76),
  (108,"bob","Mumbai",65),
  (124,"casey","pune",94),
  (112,"duke","pune",80);